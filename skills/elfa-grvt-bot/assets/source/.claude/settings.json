{
  "_comment": "UserPromptSubmit hook surfaces pending registry alerts (triggers fired, orders placed, errors) as injected context every turn. This is the in-chat alert channel and works alongside or instead of Telegram. Edit scripts/show_pending_alerts.sh if you need to adjust behavior.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash $CLAUDE_PROJECT_DIR/scripts/show_pending_alerts.sh"
          }
        ]
      }
    ]
  }
}
